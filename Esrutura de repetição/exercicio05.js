// Imprimir números pares de 0 a 20 utilizando o loop while

let numeros = 0;

// let numeroEscolhido = parseInt(prompt("Digite um numero entre 0 e 20"))

while ( numeros <=20) {
    //Só entra no corpo se a condição for verdadeira
 console.log(numeros);
 numeros = numeros + 2
 
}

